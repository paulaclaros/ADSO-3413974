# Ejecucion local

La ejecucion local principal esta definida en `docker-compose.yml` en la raiz del producto.

```powershell
docker compose up --build
```

Servicios:

- `web`: http://localhost:5173
- `api`: http://localhost:8080
- `db`: localhost:5432

